import pandas as pd
import seaborn as sns

def line_plot(df: pd.DataFrame, cols: list[str]):
    # Assume that all the columns are in the datastructure 

    return None

def violin_chart(df: pd.DataFrame, cols: list[str]):
    return None

def bar_chart(df: pd.DataFrame, cols: list[str]):
    return None

def word_cloud(df: pd.DataFrame, col: str):
    return None